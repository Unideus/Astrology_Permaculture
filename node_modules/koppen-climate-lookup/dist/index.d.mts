interface KoppenPoint {
    latitude: number;
    longitude: number;
    koppenClass: string;
}
interface NearestPoint extends KoppenPoint {
    distance: number;
}
declare class KoppenLookup {
    private data;
    private static instance;
    private constructor();
    /**
     * Get KoppenLookup instance
     */
    static getInstance(): KoppenLookup;
    /**
     * Initialize the lookup with CSV data
     * @private
     */
    private initialize;
    /**
     * Calculate distance between two points using Haversine formula
     * @param lat1 - Latitude of first point
     * @param lon1 - Longitude of first point
     * @param lat2 - Latitude of second point
     * @param lon2 - Longitude of second point
     * @returns Distance in kilometers
     */
    private calculateDistance;
    /**
     * Validate coordinates
     * @param latitude - Latitude to validate
     * @param longitude - Longitude to validate
     * @throws Error if coordinates are invalid
     */
    private validateCoordinates;
    /**
     * Find nearest Koppen classification for given coordinates
     * @param latitude - Latitude to look up
     * @param longitude - Longitude to look up
     * @param maxDistance - Maximum distance in kilometers to search (default: 100)
     * @returns Object containing nearest point and classification, or null if none found
     * @throws Error if coordinates are invalid
     */
    findNearest(latitude: number, longitude: number, maxDistance?: number): NearestPoint | null;
}

export { KoppenLookup, KoppenLookup as default };
